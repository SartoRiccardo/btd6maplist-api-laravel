meta {
  name: PUT Read-Rules
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/read-rules
  body: none
  auth: bearer
}

headers {
  Authorization: Bearer {{USER_AUTH_TOKEN}}
}

script:post-response {
  if (res.status === 204) {
    console.log("Rules marked as read");
  }
}
