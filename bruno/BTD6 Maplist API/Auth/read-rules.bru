meta {
  name: Read Rules
  type: http
  seq: 1
}

put {
  url: {{BASE_URL}}/api/read-rules
  body: none
  auth: bearer
}

auth:bearer {
  token: {{USER_AUTH_TOKEN}}
}

headers {
  Accept: application/json
}
