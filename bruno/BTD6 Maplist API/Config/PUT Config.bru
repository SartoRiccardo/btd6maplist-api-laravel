meta {
  name: PUT Config
  type: http
  seq: 2
}

put {
  url: {{BASE_URL}}/config
  body: json
  auth: bearer
}

headers {
  Authorization: Bearer {{USER_AUTH_TOKEN}}
}

body:json {
  {
    "config": {
      "points_top_map": 150,
      "points_bottom_map": 10
    }
  }
}
